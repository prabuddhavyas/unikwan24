{
  "name": "unikwan24",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:tokens": "npx style-dictionary build",
    "tokens:extract": "node extract-tokens.mjs",
    "tokens:build": "style-dictionary build",
    "build": "npm run tokens:extract && npm run tokens:build"
  }
}